export class User {
    constructor(public id:number, public name:string, public isadmin:boolean, public email:string, public password:string, public firstname:string, public lastname:string, public street:string, public street_number:number, public zip:number, public city:string, public country:string) {

    }
}
